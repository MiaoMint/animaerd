// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/MiaoMint/animaerd/ent/aspectratio"
)

// AspectRatio is the model entity for the AspectRatio schema.
type AspectRatio struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Ratio holds the value of the "ratio" field.
	Ratio string `json:"ratio,omitempty"`
	// Width holds the value of the "width" field.
	Width int `json:"width,omitempty"`
	// Height holds the value of the "height" field.
	Height       int `json:"height,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AspectRatio) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case aspectratio.FieldID, aspectratio.FieldWidth, aspectratio.FieldHeight:
			values[i] = new(sql.NullInt64)
		case aspectratio.FieldRatio:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AspectRatio fields.
func (ar *AspectRatio) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case aspectratio.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ar.ID = int(value.Int64)
		case aspectratio.FieldRatio:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field ratio", values[i])
			} else if value.Valid {
				ar.Ratio = value.String
			}
		case aspectratio.FieldWidth:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field width", values[i])
			} else if value.Valid {
				ar.Width = int(value.Int64)
			}
		case aspectratio.FieldHeight:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field height", values[i])
			} else if value.Valid {
				ar.Height = int(value.Int64)
			}
		default:
			ar.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AspectRatio.
// This includes values selected through modifiers, order, etc.
func (ar *AspectRatio) Value(name string) (ent.Value, error) {
	return ar.selectValues.Get(name)
}

// Update returns a builder for updating this AspectRatio.
// Note that you need to call AspectRatio.Unwrap() before calling this method if this AspectRatio
// was returned from a transaction, and the transaction was committed or rolled back.
func (ar *AspectRatio) Update() *AspectRatioUpdateOne {
	return NewAspectRatioClient(ar.config).UpdateOne(ar)
}

// Unwrap unwraps the AspectRatio entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ar *AspectRatio) Unwrap() *AspectRatio {
	_tx, ok := ar.config.driver.(*txDriver)
	if !ok {
		panic("ent: AspectRatio is not a transactional entity")
	}
	ar.config.driver = _tx.drv
	return ar
}

// String implements the fmt.Stringer.
func (ar *AspectRatio) String() string {
	var builder strings.Builder
	builder.WriteString("AspectRatio(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ar.ID))
	builder.WriteString("ratio=")
	builder.WriteString(ar.Ratio)
	builder.WriteString(", ")
	builder.WriteString("width=")
	builder.WriteString(fmt.Sprintf("%v", ar.Width))
	builder.WriteString(", ")
	builder.WriteString("height=")
	builder.WriteString(fmt.Sprintf("%v", ar.Height))
	builder.WriteByte(')')
	return builder.String()
}

// AspectRatios is a parsable slice of AspectRatio.
type AspectRatios []*AspectRatio
