// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/MiaoMint/animaerd/ent/comfyuinode"
	"github.com/MiaoMint/animaerd/ent/predicate"
)

// ComfyUINodeQuery is the builder for querying ComfyUINode entities.
type ComfyUINodeQuery struct {
	config
	ctx        *QueryContext
	order      []comfyuinode.OrderOption
	inters     []Interceptor
	predicates []predicate.ComfyUINode
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ComfyUINodeQuery builder.
func (cunq *ComfyUINodeQuery) Where(ps ...predicate.ComfyUINode) *ComfyUINodeQuery {
	cunq.predicates = append(cunq.predicates, ps...)
	return cunq
}

// Limit the number of records to be returned by this query.
func (cunq *ComfyUINodeQuery) Limit(limit int) *ComfyUINodeQuery {
	cunq.ctx.Limit = &limit
	return cunq
}

// Offset to start from.
func (cunq *ComfyUINodeQuery) Offset(offset int) *ComfyUINodeQuery {
	cunq.ctx.Offset = &offset
	return cunq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cunq *ComfyUINodeQuery) Unique(unique bool) *ComfyUINodeQuery {
	cunq.ctx.Unique = &unique
	return cunq
}

// Order specifies how the records should be ordered.
func (cunq *ComfyUINodeQuery) Order(o ...comfyuinode.OrderOption) *ComfyUINodeQuery {
	cunq.order = append(cunq.order, o...)
	return cunq
}

// First returns the first ComfyUINode entity from the query.
// Returns a *NotFoundError when no ComfyUINode was found.
func (cunq *ComfyUINodeQuery) First(ctx context.Context) (*ComfyUINode, error) {
	nodes, err := cunq.Limit(1).All(setContextOp(ctx, cunq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{comfyuinode.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cunq *ComfyUINodeQuery) FirstX(ctx context.Context) *ComfyUINode {
	node, err := cunq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ComfyUINode ID from the query.
// Returns a *NotFoundError when no ComfyUINode ID was found.
func (cunq *ComfyUINodeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cunq.Limit(1).IDs(setContextOp(ctx, cunq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{comfyuinode.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cunq *ComfyUINodeQuery) FirstIDX(ctx context.Context) int {
	id, err := cunq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ComfyUINode entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ComfyUINode entity is found.
// Returns a *NotFoundError when no ComfyUINode entities are found.
func (cunq *ComfyUINodeQuery) Only(ctx context.Context) (*ComfyUINode, error) {
	nodes, err := cunq.Limit(2).All(setContextOp(ctx, cunq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{comfyuinode.Label}
	default:
		return nil, &NotSingularError{comfyuinode.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cunq *ComfyUINodeQuery) OnlyX(ctx context.Context) *ComfyUINode {
	node, err := cunq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ComfyUINode ID in the query.
// Returns a *NotSingularError when more than one ComfyUINode ID is found.
// Returns a *NotFoundError when no entities are found.
func (cunq *ComfyUINodeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cunq.Limit(2).IDs(setContextOp(ctx, cunq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{comfyuinode.Label}
	default:
		err = &NotSingularError{comfyuinode.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cunq *ComfyUINodeQuery) OnlyIDX(ctx context.Context) int {
	id, err := cunq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ComfyUINodes.
func (cunq *ComfyUINodeQuery) All(ctx context.Context) ([]*ComfyUINode, error) {
	ctx = setContextOp(ctx, cunq.ctx, ent.OpQueryAll)
	if err := cunq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ComfyUINode, *ComfyUINodeQuery]()
	return withInterceptors[[]*ComfyUINode](ctx, cunq, qr, cunq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cunq *ComfyUINodeQuery) AllX(ctx context.Context) []*ComfyUINode {
	nodes, err := cunq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ComfyUINode IDs.
func (cunq *ComfyUINodeQuery) IDs(ctx context.Context) (ids []int, err error) {
	if cunq.ctx.Unique == nil && cunq.path != nil {
		cunq.Unique(true)
	}
	ctx = setContextOp(ctx, cunq.ctx, ent.OpQueryIDs)
	if err = cunq.Select(comfyuinode.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cunq *ComfyUINodeQuery) IDsX(ctx context.Context) []int {
	ids, err := cunq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cunq *ComfyUINodeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cunq.ctx, ent.OpQueryCount)
	if err := cunq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cunq, querierCount[*ComfyUINodeQuery](), cunq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cunq *ComfyUINodeQuery) CountX(ctx context.Context) int {
	count, err := cunq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cunq *ComfyUINodeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cunq.ctx, ent.OpQueryExist)
	switch _, err := cunq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cunq *ComfyUINodeQuery) ExistX(ctx context.Context) bool {
	exist, err := cunq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ComfyUINodeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cunq *ComfyUINodeQuery) Clone() *ComfyUINodeQuery {
	if cunq == nil {
		return nil
	}
	return &ComfyUINodeQuery{
		config:     cunq.config,
		ctx:        cunq.ctx.Clone(),
		order:      append([]comfyuinode.OrderOption{}, cunq.order...),
		inters:     append([]Interceptor{}, cunq.inters...),
		predicates: append([]predicate.ComfyUINode{}, cunq.predicates...),
		// clone intermediate query.
		sql:  cunq.sql.Clone(),
		path: cunq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ComfyUINode.Query().
//		GroupBy(comfyuinode.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cunq *ComfyUINodeQuery) GroupBy(field string, fields ...string) *ComfyUINodeGroupBy {
	cunq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ComfyUINodeGroupBy{build: cunq}
	grbuild.flds = &cunq.ctx.Fields
	grbuild.label = comfyuinode.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.ComfyUINode.Query().
//		Select(comfyuinode.FieldCreateTime).
//		Scan(ctx, &v)
func (cunq *ComfyUINodeQuery) Select(fields ...string) *ComfyUINodeSelect {
	cunq.ctx.Fields = append(cunq.ctx.Fields, fields...)
	sbuild := &ComfyUINodeSelect{ComfyUINodeQuery: cunq}
	sbuild.label = comfyuinode.Label
	sbuild.flds, sbuild.scan = &cunq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ComfyUINodeSelect configured with the given aggregations.
func (cunq *ComfyUINodeQuery) Aggregate(fns ...AggregateFunc) *ComfyUINodeSelect {
	return cunq.Select().Aggregate(fns...)
}

func (cunq *ComfyUINodeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cunq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cunq); err != nil {
				return err
			}
		}
	}
	for _, f := range cunq.ctx.Fields {
		if !comfyuinode.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cunq.path != nil {
		prev, err := cunq.path(ctx)
		if err != nil {
			return err
		}
		cunq.sql = prev
	}
	return nil
}

func (cunq *ComfyUINodeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ComfyUINode, error) {
	var (
		nodes = []*ComfyUINode{}
		_spec = cunq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ComfyUINode).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ComfyUINode{config: cunq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cunq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (cunq *ComfyUINodeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cunq.querySpec()
	_spec.Node.Columns = cunq.ctx.Fields
	if len(cunq.ctx.Fields) > 0 {
		_spec.Unique = cunq.ctx.Unique != nil && *cunq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cunq.driver, _spec)
}

func (cunq *ComfyUINodeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(comfyuinode.Table, comfyuinode.Columns, sqlgraph.NewFieldSpec(comfyuinode.FieldID, field.TypeInt))
	_spec.From = cunq.sql
	if unique := cunq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cunq.path != nil {
		_spec.Unique = true
	}
	if fields := cunq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, comfyuinode.FieldID)
		for i := range fields {
			if fields[i] != comfyuinode.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cunq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cunq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cunq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cunq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cunq *ComfyUINodeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cunq.driver.Dialect())
	t1 := builder.Table(comfyuinode.Table)
	columns := cunq.ctx.Fields
	if len(columns) == 0 {
		columns = comfyuinode.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cunq.sql != nil {
		selector = cunq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cunq.ctx.Unique != nil && *cunq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cunq.predicates {
		p(selector)
	}
	for _, p := range cunq.order {
		p(selector)
	}
	if offset := cunq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cunq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ComfyUINodeGroupBy is the group-by builder for ComfyUINode entities.
type ComfyUINodeGroupBy struct {
	selector
	build *ComfyUINodeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cungb *ComfyUINodeGroupBy) Aggregate(fns ...AggregateFunc) *ComfyUINodeGroupBy {
	cungb.fns = append(cungb.fns, fns...)
	return cungb
}

// Scan applies the selector query and scans the result into the given value.
func (cungb *ComfyUINodeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cungb.build.ctx, ent.OpQueryGroupBy)
	if err := cungb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ComfyUINodeQuery, *ComfyUINodeGroupBy](ctx, cungb.build, cungb, cungb.build.inters, v)
}

func (cungb *ComfyUINodeGroupBy) sqlScan(ctx context.Context, root *ComfyUINodeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cungb.fns))
	for _, fn := range cungb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cungb.flds)+len(cungb.fns))
		for _, f := range *cungb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cungb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cungb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ComfyUINodeSelect is the builder for selecting fields of ComfyUINode entities.
type ComfyUINodeSelect struct {
	*ComfyUINodeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cuns *ComfyUINodeSelect) Aggregate(fns ...AggregateFunc) *ComfyUINodeSelect {
	cuns.fns = append(cuns.fns, fns...)
	return cuns
}

// Scan applies the selector query and scans the result into the given value.
func (cuns *ComfyUINodeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cuns.ctx, ent.OpQuerySelect)
	if err := cuns.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ComfyUINodeQuery, *ComfyUINodeSelect](ctx, cuns.ComfyUINodeQuery, cuns, cuns.inters, v)
}

func (cuns *ComfyUINodeSelect) sqlScan(ctx context.Context, root *ComfyUINodeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cuns.fns))
	for _, fn := range cuns.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cuns.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cuns.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
